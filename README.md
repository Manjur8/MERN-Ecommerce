Pull: "git clone https://github.com/Manjur8/MERN-Ecommerce.git"

To run the project:

Backend:

  to run the backend:
   1. npm install -g typescript
   2. npm run build
   3. "npm run watch" in one terminal & "npm run dev" in other terminal (Note: both terminal should be running)
